const API_BASE_URL = 'http://0.0.0.0:8080';

const API_URLS = {
  FETCH_MANCALA: `${API_BASE_URL}/mancala`,
  PLAY_MOVE: `${API_BASE_URL}/play`,
  RESET: `${API_BASE_URL}/reset`
};

export default API_URLS;
